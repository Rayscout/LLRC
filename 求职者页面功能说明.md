# 求职者页面功能说明

## 概述
重新设计的求职者仪表板页面，确保每个按钮都能实现实际功能，提供完整的求职体验。

## 主要功能模块

### 1. 仪表板首页 (`candidate_dashboard.html`)
**路径**: `/smartrecruit/candidate/dashboard`

#### 功能特性：
- **欢迎横幅**: 显示用户信息和快速统计数据
- **技能展示**: 动态显示用户技能标签
- **推荐职位**: 基于用户技能智能推荐职位
- **快速操作**: 6个主要功能入口
- **最近活动**: 用户操作历史记录
- **技能提升建议**: 个性化学习建议

#### 新增功能：
- 实时统计数据更新
- 技能更新和分析功能
- 职位收藏功能
- 活动记录管理
- 学习资源推荐

### 2. API端点

#### 仪表板统计API
- **路径**: `/smartrecruit/candidate/api/dashboard-stats`
- **方法**: GET
- **功能**: 获取用户统计数据（申请数量、收藏职位、面试次数等）

#### 推荐职位API
- **路径**: `/smartrecruit/candidate/api/recommended-jobs`
- **方法**: GET
- **功能**: 获取基于用户技能的推荐职位

#### 用户技能API
- **路径**: `/smartrecruit/candidate/api/user-skills`
- **方法**: GET
- **功能**: 获取用户当前技能列表

#### 收藏职位API
- **路径**: `/smartrecruit/candidate/api/save-job`
- **方法**: POST
- **功能**: 收藏指定职位

#### 简历分析API
- **路径**: `/smartrecruit/candidate/api/analyze-resume`
- **方法**: GET
- **功能**: 分析用户上传的简历

#### 更新技能API
- **路径**: `/smartrecruit/candidate/api/update-skills`
- **方法**: POST
- **功能**: 更新用户技能标签

### 3. 快速操作功能

#### 搜索职位
- **链接**: 跳转到职位搜索页面
- **功能**: 基于技能和偏好搜索职位
- **统计**: 显示今日新增职位数量

#### 上传简历
- **链接**: 跳转到个人设置页面
- **功能**: 支持多种格式（PDF、Word、图片、视频）
- **状态**: 显示简历上传状态

#### 我的申请
- **链接**: 跳转到申请管理页面
- **功能**: 查看和管理所有职位申请
- **统计**: 显示待处理申请数量

#### 职业发展
- **链接**: 跳转到职业发展页面
- **功能**: 技能差距分析和学习建议
- **统计**: 显示技能匹配度

#### AI面试
- **链接**: 跳转到AI面试页面
- **功能**: 智能面试练习和反馈
- **统计**: 显示练习次数

#### 申请分析
- **链接**: 跳转到申请分析页面
- **功能**: 申请成功率分析和改进建议
- **统计**: 显示申请成功率

### 4. 新增后端函数

#### Jobs模块 (`jobs.py`)
- `get_new_jobs_count()`: 获取今日新增职位数量
- `get_recommended_jobs(user_id)`: 获取推荐职位列表
- `save_job_for_user(user_id, job_id)`: 收藏职位
- `get_job_details(job_id)`: 获取职位详情
- `search_jobs_by_filters(filters)`: 根据筛选条件搜索职位
- `get_popular_jobs()`: 获取热门职位
- `get_job_statistics()`: 获取职位统计信息

#### Applications模块 (`applications.py`)
- `get_pending_applications_count(user_id)`: 获取待处理申请数量
- `get_success_rate(user_id)`: 获取申请成功率
- `get_user_applications(user_id, limit)`: 获取用户申请列表
- `get_application_statistics(user_id)`: 获取申请统计信息
- `get_recent_applications(user_id, limit)`: 获取最近的申请
- `get_application_trends(user_id, days)`: 获取申请趋势

#### Interview模块 (`interview.py`)
- `get_interview_count(user_id)`: 获取面试练习次数
- `get_interview_statistics(user_id)`: 获取面试统计信息
- `get_recent_interviews(user_id, limit)`: 获取最近的面试记录
- `get_interview_performance_trends(user_id, days)`: 获取面试表现趋势
- `get_interview_skill_analysis(user_id)`: 获取面试技能分析
- `categorize_question_skill(question)`: 根据问题内容分类技能
- `extract_score_from_feedback(feedback)`: 从反馈中提取分数

#### Profile模块 (`profile.py`)
- `update_user_skills(user_id, skills)`: 更新用户技能
- `get_user_profile_completion(user_id)`: 获取用户资料完整度
- `get_user_profile_data(user_id)`: 获取用户资料数据
- `get_skill_recommendations(user_id)`: 获取技能推荐
- `get_career_path_suggestions(user_id)`: 获取职业发展建议
- `get_learning_resources(skill)`: 获取学习资源

### 5. 前端交互功能

#### JavaScript功能
- `initializeDashboard()`: 初始化仪表板数据
- `loadDashboardStats()`: 加载统计数据
- `loadRecommendedJobs()`: 加载推荐职位
- `saveJob(jobId)`: 收藏职位
- `updateSkills()`: 更新技能
- `analyzeResume()`: 分析简历
- `loadMoreActivities()`: 加载更多活动
- `clearActivities()`: 清除活动记录
- `startLearning(topic)`: 开始学习
- `viewResources(topic)`: 查看学习资源

#### 实时更新
- 每5分钟自动刷新统计数据
- 动态更新推荐职位
- 实时显示用户活动

### 6. 测试页面

#### 功能测试页面
- **路径**: `/smartrecruit/candidate/test`
- **功能**: 测试所有API端点和功能
- **特性**: 
  - API端点测试
  - 页面跳转测试
  - 功能测试
  - 系统状态检查

### 7. 样式和用户体验

#### 设计特点
- 现代化UI设计
- 响应式布局
- 流畅的动画效果
- 直观的图标和颜色
- 清晰的信息层次

#### 交互体验
- 悬停效果
- 点击反馈
- 加载状态提示
- 错误处理
- 成功提示

### 8. 数据流

#### 数据获取流程
1. 页面加载时初始化数据
2. 调用API获取实时数据
3. 更新页面显示
4. 定期刷新保持数据最新

#### 用户操作流程
1. 用户点击按钮
2. 前端验证和预处理
3. 发送API请求
4. 后端处理并返回结果
5. 前端更新界面
6. 显示操作结果

### 9. 错误处理

#### 前端错误处理
- API请求失败提示
- 网络连接错误处理
- 用户输入验证
- 友好的错误信息

#### 后端错误处理
- 数据库连接错误
- 文件上传错误
- 权限验证错误
- 异常捕获和日志记录

### 10. 安全性

#### 用户认证
- 登录状态检查
- 权限验证
- 会话管理

#### 数据安全
- 输入验证
- SQL注入防护
- XSS防护
- CSRF防护

## 总结

重新设计的求职者页面提供了完整的功能体验：

1. **功能完整性**: 每个按钮都有对应的实际功能
2. **数据实时性**: 通过API实时获取和更新数据
3. **用户体验**: 现代化的UI设计和流畅的交互
4. **可扩展性**: 模块化设计便于后续功能扩展
5. **可维护性**: 清晰的代码结构和完善的错误处理
6. **可测试性**: 提供专门的测试页面验证功能

所有功能都已经实现并可以正常工作，用户可以通过仪表板完成求职过程中的各种操作。


