"""
LLRC Header Start
文件功能: 通用 Python 脚本/模块：Code/LLRC/.venv/Lib/site-packages/pip/_vendor/pyproject_hooks/_in_process/__init__.py
创建时间: 2025-08-21 16:24
创建人: 谢佳悦
更新记录:
- 2025-08-21 16:54 by 潘显雨
- 2025-08-23 15:19 by 潘显雨
LLRC Header End
"""
"""This is a subpackage because the directory is on sys.path for _in_process.py

The subpackage should stay as empty as possible to avoid shadowing modules that
the backend might import.
"""

import importlib.resources as resources

try:
    resources.files
except AttributeError:
    # Python 3.8 compatibility
    def _in_proc_script_path():
        """函数 _in_proc_script_path：核心业务逻辑。"""
        return resources.path(__package__, "_in_process.py")

else:

    def _in_proc_script_path():
        """函数 _in_proc_script_path：核心业务逻辑。"""
        return resources.as_file(
            resources.files(__package__).joinpath("_in_process.py")
        )
