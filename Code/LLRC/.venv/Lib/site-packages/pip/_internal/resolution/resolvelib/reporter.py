"""
LLRC Header Start
文件功能: 通用 Python 脚本/模块：Code/LLRC/.venv/Lib/site-packages/pip/_internal/resolution/resolvelib/reporter.py
创建时间: 2025-08-22 10:36
创建人: 谢佳悦
更新记录:
- 2025-08-22 11:06 by 侯东杨
- 2025-08-26 14:24 by 侯东杨
- 2025-09-02 12:55 by 苏杰
LLRC Header End
"""
from __future__ import annotations

from collections import defaultdict
from logging import getLogger
from typing import Any

from pip._vendor.resolvelib.reporters import BaseReporter

from .base import Candidate, Requirement

logger = getLogger(__name__)


class PipReporter(BaseReporter[Requirement, Candidate, str]):
    """类 PipReporter：封装与该模块相关的数据与行为。"""
    def __init__(self) -> None:
        """函数 __init__：核心业务逻辑。"""
        self.reject_count_by_package: defaultdict[str, int] = defaultdict(int)

        self._messages_at_reject_count = {
            1: (
                "pip is looking at multiple versions of {package_name} to "
                "determine which version is compatible with other "
                "requirements. This could take a while."
            ),
            8: (
                "pip is still looking at multiple versions of {package_name} to "
                "determine which version is compatible with other "
                "requirements. This could take a while."
            ),
            13: (
                "This is taking longer than usual. You might need to provide "
                "the dependency resolver with stricter constraints to reduce "
                "runtime. See https://pip.pypa.io/warnings/backtracking for "
                "guidance. If you want to abort this run, press Ctrl + C."
            ),
        }

    def rejecting_candidate(self, criterion: Any, candidate: Candidate) -> None:
        """函数 rejecting_candidate：处理 criterion, candidate 相关逻辑。"""
        self.reject_count_by_package[candidate.name] += 1

        count = self.reject_count_by_package[candidate.name]
        if count not in self._messages_at_reject_count:
            return

        message = self._messages_at_reject_count[count]
        logger.info("INFO: %s", message.format(package_name=candidate.name))

        msg = "Will try a different candidate, due to conflict:"
        for req_info in criterion.information:
            req, parent = req_info.requirement, req_info.parent
            # Inspired by Factory.get_installation_error
            msg += "\n    "
            if parent:
                msg += f"{parent.name} {parent.version} depends on "
            else:
                msg += "The user requested "
            msg += req.format_for_error()
        logger.debug(msg)


class PipDebuggingReporter(BaseReporter[Requirement, Candidate, str]):
    """A reporter that does an info log for every event it sees."""

    def starting(self) -> None:
        """函数 starting：核心业务逻辑。"""
        logger.info("Reporter.starting()")

    def starting_round(self, index: int) -> None:
        """函数 starting_round：处理 index 相关逻辑。"""
        logger.info("Reporter.starting_round(%r)", index)

    def ending_round(self, index: int, state: Any) -> None:
        """函数 ending_round：处理 index, state 相关逻辑。"""
        logger.info("Reporter.ending_round(%r, state)", index)
        logger.debug("Reporter.ending_round(%r, %r)", index, state)

    def ending(self, state: Any) -> None:
        """函数 ending：处理 state 相关逻辑。"""
        logger.info("Reporter.ending(%r)", state)

    def adding_requirement(
        """函数 adding_requirement：处理 requirement, parent 相关逻辑。"""
        self, requirement: Requirement, parent: Candidate | None
    ) -> None:
        logger.info("Reporter.adding_requirement(%r, %r)", requirement, parent)

    def rejecting_candidate(self, criterion: Any, candidate: Candidate) -> None:
        """函数 rejecting_candidate：处理 criterion, candidate 相关逻辑。"""
        logger.info("Reporter.rejecting_candidate(%r, %r)", criterion, candidate)

    def pinning(self, candidate: Candidate) -> None:
        """函数 pinning：处理 candidate 相关逻辑。"""
        logger.info("Reporter.pinning(%r)", candidate)
