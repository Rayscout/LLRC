"""
LLRC Header Start
文件功能: 通用 Python 脚本/模块：Code/LLRC/.venv/Lib/site-packages/setuptools/_vendor/pyparsing/util.py
创建时间: 2025-08-25 14:02
创建人: 李雨梦
更新记录:
- 2025-08-25 14:32 by 苏杰
- 2025-08-26 17:36 by 李雨梦
- 2025-09-02 10:40 by 李雨梦
LLRC Header End
"""
# util.py
import warnings
import types
import collections
import itertools
from functools import lru_cache
from typing import List, Union, Iterable

_bslash = chr(92)


class __config_flags:
    """Internal class for defining compatibility and debugging flags"""

    _all_names: List[str] = []
    _fixed_names: List[str] = []
    _type_desc = "configuration"

    @classmethod
    def _set(cls, dname, value):
        """函数 _set：处理 dname, value 相关逻辑。"""
        if dname in cls._fixed_names:
            warnings.warn(
                "{}.{} {} is {} and cannot be overridden".format(
                    cls.__name__,
                    dname,
                    cls._type_desc,
                    str(getattr(cls, dname)).upper(),
                )
            )
            return
        if dname in cls._all_names:
            setattr(cls, dname, value)
        else:
            raise ValueError("no such {} {!r}".format(cls._type_desc, dname))

    enable = classmethod(lambda cls, name: cls._set(name, True))
    disable = classmethod(lambda cls, name: cls._set(name, False))


@lru_cache(maxsize=128)
def col(loc: int, strg: str) -> int:
    """
    Returns current column within a string, counting newlines as line separators.
    The first column is number 1.

    Note: the default parsing behavior is to expand tabs in the input string
    before starting the parsing process.  See
    :class:`ParserElement.parseString` for more
    information on parsing strings containing ``<TAB>`` s, and suggested
    methods to maintain a consistent view of the parsed string, the parse
    location, and line and column positions within the parsed string.
    """
    s = strg
    return 1 if 0 < loc < len(s) and s[loc - 1] == "\n" else loc - s.rfind("\n", 0, loc)


@lru_cache(maxsize=128)
def lineno(loc: int, strg: str) -> int:
    """Returns current line number within a string, counting newlines as line separators.
    The first line is number 1.

    Note - the default parsing behavior is to expand tabs in the input string
    before starting the parsing process.  See :class:`ParserElement.parseString`
    for more information on parsing strings containing ``<TAB>`` s, and
    suggested methods to maintain a consistent view of the parsed string, the
    parse location, and line and column positions within the parsed string.
    """
    return strg.count("\n", 0, loc) + 1


@lru_cache(maxsize=128)
def line(loc: int, strg: str) -> str:
    """
    Returns the line of text containing loc within a string, counting newlines as line separators.
    """
    last_cr = strg.rfind("\n", 0, loc)
    next_cr = strg.find("\n", loc)
    return strg[last_cr + 1 : next_cr] if next_cr >= 0 else strg[last_cr + 1 :]


class _UnboundedCache:
    """类 _UnboundedCache：封装与该模块相关的数据与行为。"""
    def __init__(self):
        """函数 __init__：核心业务逻辑。"""
        cache = {}
        cache_get = cache.get
        self.not_in_cache = not_in_cache = object()

        def get(_, key):
            """函数 get：处理 _, key 相关逻辑。"""
            return cache_get(key, not_in_cache)

        def set_(_, key, value):
            """函数 set_：处理 _, key, value 相关逻辑。"""
            cache[key] = value

        def clear(_):
            """函数 clear：处理 _ 相关逻辑。"""
            cache.clear()

        self.size = None
        self.get = types.MethodType(get, self)
        self.set = types.MethodType(set_, self)
        self.clear = types.MethodType(clear, self)


class _FifoCache:
    """类 _FifoCache：封装与该模块相关的数据与行为。"""
    def __init__(self, size):
        """函数 __init__：处理 size 相关逻辑。"""
        self.not_in_cache = not_in_cache = object()
        cache = collections.OrderedDict()
        cache_get = cache.get

        def get(_, key):
            """函数 get：处理 _, key 相关逻辑。"""
            return cache_get(key, not_in_cache)

        def set_(_, key, value):
            """函数 set_：处理 _, key, value 相关逻辑。"""
            cache[key] = value
            while len(cache) > size:
                cache.popitem(last=False)

        def clear(_):
            """函数 clear：处理 _ 相关逻辑。"""
            cache.clear()

        self.size = size
        self.get = types.MethodType(get, self)
        self.set = types.MethodType(set_, self)
        self.clear = types.MethodType(clear, self)


class LRUMemo:
    """
    A memoizing mapping that retains `capacity` deleted items

    The memo tracks retained items by their access order; once `capacity` items
    are retained, the least recently used item is discarded.
    """

    def __init__(self, capacity):
        """函数 __init__：处理 capacity 相关逻辑。"""
        self._capacity = capacity
        self._active = {}
        self._memory = collections.OrderedDict()

    def __getitem__(self, key):
        """函数 __getitem__：处理 key 相关逻辑。"""
        try:
            return self._active[key]
        except KeyError:
            self._memory.move_to_end(key)
            return self._memory[key]

    def __setitem__(self, key, value):
        """函数 __setitem__：处理 key, value 相关逻辑。"""
        self._memory.pop(key, None)
        self._active[key] = value

    def __delitem__(self, key):
        """函数 __delitem__：处理 key 相关逻辑。"""
        try:
            value = self._active.pop(key)
        except KeyError:
            pass
        else:
            while len(self._memory) >= self._capacity:
                self._memory.popitem(last=False)
            self._memory[key] = value

    def clear(self):
        """函数 clear：核心业务逻辑。"""
        self._active.clear()
        self._memory.clear()


class UnboundedMemo(dict):
    """
    A memoizing mapping that retains all deleted items
    """

    def __delitem__(self, key):
        """函数 __delitem__：处理 key 相关逻辑。"""
        pass


def _escape_regex_range_chars(s: str) -> str:
    """函数 _escape_regex_range_chars：处理 s 相关逻辑。"""
    # escape these chars: ^-[]
    for c in r"\^-[]":
        s = s.replace(c, _bslash + c)
    s = s.replace("\n", r"\n")
    s = s.replace("\t", r"\t")
    return str(s)


def _collapse_string_to_ranges(
    """函数 _collapse_string_to_ranges：处理 s, re_escape 相关逻辑。"""
    s: Union[str, Iterable[str]], re_escape: bool = True
) -> str:
    def is_consecutive(c):
        """函数 is_consecutive：处理 c 相关逻辑。"""
        c_int = ord(c)
        is_consecutive.prev, prev = c_int, is_consecutive.prev
        if c_int - prev > 1:
            is_consecutive.value = next(is_consecutive.counter)
        return is_consecutive.value

    is_consecutive.prev = 0
    is_consecutive.counter = itertools.count()
    is_consecutive.value = -1

    def escape_re_range_char(c):
        """函数 escape_re_range_char：处理 c 相关逻辑。"""
        return "\\" + c if c in r"\^-][" else c

    def no_escape_re_range_char(c):
        """函数 no_escape_re_range_char：处理 c 相关逻辑。"""
        return c

    if not re_escape:
        escape_re_range_char = no_escape_re_range_char

    ret = []
    s = "".join(sorted(set(s)))
    if len(s) > 3:
        for _, chars in itertools.groupby(s, key=is_consecutive):
            first = last = next(chars)
            last = collections.deque(
                itertools.chain(iter([last]), chars), maxlen=1
            ).pop()
            if first == last:
                ret.append(escape_re_range_char(first))
            else:
                sep = "" if ord(last) == ord(first) + 1 else "-"
                ret.append(
                    "{}{}{}".format(
                        escape_re_range_char(first), sep, escape_re_range_char(last)
                    )
                )
    else:
        ret = [escape_re_range_char(c) for c in s]

    return "".join(ret)


def _flatten(ll: list) -> list:
    """函数 _flatten：处理 ll 相关逻辑。"""
    ret = []
    for i in ll:
        if isinstance(i, list):
            ret.extend(_flatten(i))
        else:
            ret.append(i)
    return ret
