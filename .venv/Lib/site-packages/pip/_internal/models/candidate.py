"""
LLRC Header Start
文件功能: 通用 Python 脚本/模块：.venv/Lib/site-packages/pip/_internal/models/candidate.py
创建时间: 2025-08-20 11:01
创建人: 李雨梦
更新记录:
- 2025-08-20 11:31 by 李雨梦
- 2025-08-20 14:54 by 李雨梦
- 2025-08-23 12:23 by 侯东杨
LLRC Header End
"""
from dataclasses import dataclass

from pip._vendor.packaging.version import Version
from pip._vendor.packaging.version import parse as parse_version

from pip._internal.models.link import Link


@dataclass(frozen=True)
class InstallationCandidate:
    """Represents a potential "candidate" for installation."""

    __slots__ = ["name", "version", "link"]

    name: str
    version: Version
    link: Link

    def __init__(self, name: str, version: str, link: Link) -> None:
        """函数 __init__：处理 name, version, link 相关逻辑。"""
        object.__setattr__(self, "name", name)
        object.__setattr__(self, "version", parse_version(version))
        object.__setattr__(self, "link", link)

    def __str__(self) -> str:
        """函数 __str__：核心业务逻辑。"""
        return f"{self.name!r} candidate (version {self.version} at {self.link})"
