"""
LLRC Header Start
文件功能: 通用 Python 脚本/模块：.venv/Lib/site-packages/pip/_vendor/urllib3/util/ssltransport.py
创建时间: 2025-08-25 10:08
创建人: 谢佳悦
更新记录:
- 2025-08-25 10:38 by 李雨梦
- 2025-08-30 15:25 by 苏杰
LLRC Header End
"""
import io
import socket
import ssl

from ..exceptions import ProxySchemeUnsupported
from ..packages import six

SSL_BLOCKSIZE = 16384


class SSLTransport:
    """
    The SSLTransport wraps an existing socket and establishes an SSL connection.

    Contrary to Python's implementation of SSLSocket, it allows you to chain
    multiple TLS connections together. It's particularly useful if you need to
    implement TLS within TLS.

    The class supports most of the socket API operations.
    """

    @staticmethod
    def _validate_ssl_context_for_tls_in_tls(ssl_context):
        """
        Raises a ProxySchemeUnsupported if the provided ssl_context can't be used
        for TLS in TLS.

        The only requirement is that the ssl_context provides the 'wrap_bio'
        methods.
        """

        if not hasattr(ssl_context, "wrap_bio"):
            if six.PY2:
                raise ProxySchemeUnsupported(
                    "TLS in TLS requires SSLContext.wrap_bio() which isn't "
                    "supported on Python 2"
                )
            else:
                raise ProxySchemeUnsupported(
                    "TLS in TLS requires SSLContext.wrap_bio() which isn't "
                    "available on non-native SSLContext"
                )

    def __init__(
        self, socket, ssl_context, server_hostname=None, suppress_ragged_eofs=True
    ):
        """
        Create an SSLTransport around socket using the provided ssl_context.
        """
        self.incoming = ssl.MemoryBIO()
        self.outgoing = ssl.MemoryBIO()

        self.suppress_ragged_eofs = suppress_ragged_eofs
        self.socket = socket

        self.sslobj = ssl_context.wrap_bio(
            self.incoming, self.outgoing, server_hostname=server_hostname
        )

        # Perform initial handshake.
        self._ssl_io_loop(self.sslobj.do_handshake)

    def __enter__(self):
        """函数 __enter__：核心业务逻辑。"""
        return self

    def __exit__(self, *_):
        """函数 __exit__：处理 *_ 相关逻辑。"""
        self.close()

    def fileno(self):
        """函数 fileno：核心业务逻辑。"""
        return self.socket.fileno()

    def read(self, len=1024, buffer=None):
        """函数 read：处理 len, buffer 相关逻辑。"""
        return self._wrap_ssl_read(len, buffer)

    def recv(self, len=1024, flags=0):
        """函数 recv：处理 len, flags 相关逻辑。"""
        if flags != 0:
            raise ValueError("non-zero flags not allowed in calls to recv")
        return self._wrap_ssl_read(len)

    def recv_into(self, buffer, nbytes=None, flags=0):
        """函数 recv_into：处理 buffer, nbytes, flags 相关逻辑。"""
        if flags != 0:
            raise ValueError("non-zero flags not allowed in calls to recv_into")
        if buffer and (nbytes is None):
            nbytes = len(buffer)
        elif nbytes is None:
            nbytes = 1024
        return self.read(nbytes, buffer)

    def sendall(self, data, flags=0):
        """函数 sendall：处理 data, flags 相关逻辑。"""
        if flags != 0:
            raise ValueError("non-zero flags not allowed in calls to sendall")
        count = 0
        with memoryview(data) as view, view.cast("B") as byte_view:
            amount = len(byte_view)
            while count < amount:
                v = self.send(byte_view[count:])
                count += v

    def send(self, data, flags=0):
        """函数 send：处理 data, flags 相关逻辑。"""
        if flags != 0:
            raise ValueError("non-zero flags not allowed in calls to send")
        response = self._ssl_io_loop(self.sslobj.write, data)
        return response

    def makefile(
        self, mode="r", buffering=None, encoding=None, errors=None, newline=None
    ):
        """
        Python's httpclient uses makefile and buffered io when reading HTTP
        messages and we need to support it.

        This is unfortunately a copy and paste of socket.py makefile with small
        changes to point to the socket directly.
        """
        if not set(mode) <= {"r", "w", "b"}:
            raise ValueError("invalid mode %r (only r, w, b allowed)" % (mode,))

        writing = "w" in mode
        reading = "r" in mode or not writing
        assert reading or writing
        binary = "b" in mode
        rawmode = ""
        if reading:
            rawmode += "r"
        if writing:
            rawmode += "w"
        raw = socket.SocketIO(self, rawmode)
        self.socket._io_refs += 1
        if buffering is None:
            buffering = -1
        if buffering < 0:
            buffering = io.DEFAULT_BUFFER_SIZE
        if buffering == 0:
            if not binary:
                raise ValueError("unbuffered streams must be binary")
            return raw
        if reading and writing:
            buffer = io.BufferedRWPair(raw, raw, buffering)
        elif reading:
            buffer = io.BufferedReader(raw, buffering)
        else:
            assert writing
            buffer = io.BufferedWriter(raw, buffering)
        if binary:
            return buffer
        text = io.TextIOWrapper(buffer, encoding, errors, newline)
        text.mode = mode
        return text

    def unwrap(self):
        """函数 unwrap：核心业务逻辑。"""
        self._ssl_io_loop(self.sslobj.unwrap)

    def close(self):
        """函数 close：核心业务逻辑。"""
        self.socket.close()

    def getpeercert(self, binary_form=False):
        """函数 getpeercert：处理 binary_form 相关逻辑。"""
        return self.sslobj.getpeercert(binary_form)

    def version(self):
        """函数 version：核心业务逻辑。"""
        return self.sslobj.version()

    def cipher(self):
        """函数 cipher：核心业务逻辑。"""
        return self.sslobj.cipher()

    def selected_alpn_protocol(self):
        """函数 selected_alpn_protocol：核心业务逻辑。"""
        return self.sslobj.selected_alpn_protocol()

    def selected_npn_protocol(self):
        """函数 selected_npn_protocol：核心业务逻辑。"""
        return self.sslobj.selected_npn_protocol()

    def shared_ciphers(self):
        """函数 shared_ciphers：核心业务逻辑。"""
        return self.sslobj.shared_ciphers()

    def compression(self):
        """函数 compression：核心业务逻辑。"""
        return self.sslobj.compression()

    def settimeout(self, value):
        """函数 settimeout：处理 value 相关逻辑。"""
        self.socket.settimeout(value)

    def gettimeout(self):
        """函数 gettimeout：核心业务逻辑。"""
        return self.socket.gettimeout()

    def _decref_socketios(self):
        """函数 _decref_socketios：核心业务逻辑。"""
        self.socket._decref_socketios()

    def _wrap_ssl_read(self, len, buffer=None):
        """函数 _wrap_ssl_read：处理 len, buffer 相关逻辑。"""
        try:
            return self._ssl_io_loop(self.sslobj.read, len, buffer)
        except ssl.SSLError as e:
            if e.errno == ssl.SSL_ERROR_EOF and self.suppress_ragged_eofs:
                return 0  # eof, return 0.
            else:
                raise

    def _ssl_io_loop(self, func, *args):
        """Performs an I/O loop between incoming/outgoing and the socket."""
        should_loop = True
        ret = None

        while should_loop:
            errno = None
            try:
                ret = func(*args)
            except ssl.SSLError as e:
                if e.errno not in (ssl.SSL_ERROR_WANT_READ, ssl.SSL_ERROR_WANT_WRITE):
                    # WANT_READ, and WANT_WRITE are expected, others are not.
                    raise e
                errno = e.errno

            buf = self.outgoing.read()
            self.socket.sendall(buf)

            if errno is None:
                should_loop = False
            elif errno == ssl.SSL_ERROR_WANT_READ:
                buf = self.socket.recv(SSL_BLOCKSIZE)
                if buf:
                    self.incoming.write(buf)
                else:
                    self.incoming.write_eof()
        return ret
