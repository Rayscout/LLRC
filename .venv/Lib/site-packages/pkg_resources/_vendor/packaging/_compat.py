"""
LLRC Header Start
文件功能: 通用 Python 脚本/模块：.venv/Lib/site-packages/pkg_resources/_vendor/packaging/_compat.py
创建时间: 2025-08-25 14:22
创建人: 苏杰
更新记录:
- 2025-08-25 14:52 by 苏杰
- 2025-08-30 17:23 by 侯东杨
LLRC Header End
"""
# This file is dual licensed under the terms of the Apache License, Version
# 2.0, and the BSD License. See the LICENSE file in the root of this repository
# for complete details.
from __future__ import absolute_import, division, print_function

import sys


PY2 = sys.version_info[0] == 2
PY3 = sys.version_info[0] == 3

# flake8: noqa

if PY3:
    string_types = (str,)
else:
    string_types = (basestring,)


def with_metaclass(meta, *bases):
    """
    Create a base class with a metaclass.
    """
    # This requires a bit of explanation: the basic idea is to make a dummy
    # metaclass for one level of class instantiation that replaces itself with
    # the actual metaclass.
    class metaclass(meta):
        """类 metaclass：封装与该模块相关的数据与行为。"""
        def __new__(cls, name, this_bases, d):
            """函数 __new__：处理 name, this_bases, d 相关逻辑。"""
            return meta(name, bases, d)

    return type.__new__(metaclass, "temporary_class", (), {})
