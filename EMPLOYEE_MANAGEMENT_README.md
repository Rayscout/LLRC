# 员工管理功能说明文档

## 功能概述

本系统新增了高管员工管理功能，允许高管查看、管理和注销下属员工的账号。该功能基于现有的高管-员工关系架构，确保权限控制和数据安全。

## 主要功能

### 1. 员工列表查看
- 高管可以查看所有下属员工的列表
- 显示员工基本信息：姓名、职位、部门、员工编号等
- 显示员工账号状态（活跃/已注销）
- 提供员工统计信息：总员工数、活跃员工数、已注销员工数

### 2. 员工账号注销
- 高管可以注销下属员工的账号
- 注销操作会记录操作时间和操作人
- 注销后的员工无法登录系统
- 支持软删除，保留员工数据但标记为非活跃状态

### 3. 员工账号重新激活
- 高管可以重新激活已注销的员工账号
- 重新激活后员工可以正常登录系统
- 清除注销相关的标记信息

### 4. 员工详细信息查看
- 查看员工的完整档案信息
- 包括基本信息、联系信息、账号状态、个人资料等
- 在详情页面也可以执行注销/激活操作

## 技术实现

### 数据库模型更新
在 `User` 模型中新增了以下字段：
- `is_active`: 账号是否活跃（布尔值，默认True）
- `deactivated_at`: 注销时间（日期时间）
- `deactivated_by`: 注销操作人ID（外键引用User表）

### 权限控制
- 只有高管用户才能访问员工管理功能
- 高管只能管理自己的下属员工（通过 `supervisor_id` 字段关联）
- 所有操作都有权限验证，确保数据安全

### API接口
- `GET /talent/hr_admin/employee-management/list`: 获取员工列表
- `POST /talent/hr_admin/employee-management/deactivate/<employee_id>`: 注销员工账号
- `POST /talent/hr_admin/employee-management/reactivate/<employee_id>`: 重新激活员工账号
- `GET /talent/hr_admin/employee-management/employee/<employee_id>`: 获取员工详情

## 使用方法

### 1. 访问员工管理
1. 登录高管账户
2. 在高管仪表板中点击"员工管理"导航项
3. 或点击快速操作区域的"员工管理"按钮

### 2. 查看员工列表
- 员工列表页面显示所有下属员工
- 每个员工卡片显示基本信息和状态
- 可以点击"查看详情"查看完整信息

### 3. 注销员工账号
1. 在员工列表中找到要注销的员工
2. 点击"注销账号"按钮
3. 在确认对话框中确认操作
4. 系统会显示操作结果

### 4. 重新激活员工账号
1. 在员工列表中找到已注销的员工
2. 点击"重新激活"按钮
3. 确认操作后员工账号将恢复活跃状态

## 安装和配置

### 1. 数据库迁移
运行数据库迁移脚本以添加新字段：
```bash
cd Code/LLRC
python database_migration_add_employee_management.py
```

### 2. 重启应用
确保新的模块被正确加载：
```bash
# 重启Flask应用
```

### 3. 验证功能
1. 登录高管账户
2. 访问员工管理页面
3. 检查员工列表是否正常显示
4. 测试注销和激活功能

## 注意事项

### 安全考虑
- 注销操作不可逆，请谨慎操作
- 建议在注销前与员工沟通确认
- 所有操作都会记录在系统日志中

### 数据完整性
- 注销操作采用软删除，不会物理删除员工数据
- 员工的历史记录、反馈等信息都会保留
- 可以通过重新激活恢复员工账号

### 性能考虑
- 员工列表支持分页显示（如果员工数量较多）
- 建议定期清理长期未使用的注销账号数据

## 故障排除

### 常见问题

1. **员工列表不显示**
   - 检查高管账户是否有下属员工
   - 确认员工的高管关联关系是否正确

2. **注销操作失败**
   - 检查员工是否属于当前高管
   - 确认员工账号状态
   - 查看系统错误日志

3. **权限不足错误**
   - 确认当前用户是高管身份
   - 检查会话状态是否有效

### 日志查看
系统会在控制台和日志文件中记录所有操作，包括：
- 用户访问记录
- 注销/激活操作
- 权限验证结果
- 错误信息

## 未来扩展

### 计划功能
- 批量员工管理操作
- 员工账号状态变更通知
- 员工管理操作审计日志
- 员工账号自动清理策略

### 自定义配置
- 可配置的注销原因选项
- 自定义员工状态标签
- 灵活的权限控制策略

## 技术支持

如果在使用过程中遇到问题，请：
1. 查看系统日志获取详细错误信息
2. 检查数据库连接和权限设置
3. 联系技术支持团队

---

**版本**: 1.0.0  
**更新日期**: 2024年12月  
**维护团队**: 人才管理系统开发组
