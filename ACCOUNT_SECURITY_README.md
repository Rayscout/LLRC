# 账号安全功能说明

## 🎯 功能概述

本系统新增了完整的账号安全机制，确保被注销的员工账号无法继续使用系统，已登录用户会收到通知并自动退出。

## 🔒 安全特性

### 1. 登录验证
- **状态检查**：登录时检查 `is_active` 字段
- **拒绝访问**：被注销账号无法登录系统
- **友好提示**：显示"账号已被注销"的错误信息

### 2. 会话监控
- **实时检查**：每次请求前检查账号状态
- **自动退出**：发现被注销账号立即清除会话
- **强制重定向**：自动跳转到登录页面

### 3. 状态通知
- **视觉提示**：员工仪表板显示账号状态
- **实时更新**：状态变化时立即反映在界面上
- **自动检查**：JavaScript定期检查账号状态

## 🚀 使用方法

### 高管操作
1. 登录高管账号
2. 访问"员工管理"页面
3. 选择要注销的员工
4. 点击"注销账号"按钮
5. 确认操作

### 员工体验
1. **正常状态**：显示绿色"账号状态：活跃"
2. **注销状态**：显示红色"账号状态：已注销"
3. **自动退出**：被注销后3秒内自动退出
4. **状态通知**：右上角显示注销通知

## 🔧 技术实现

### 后端
- **登录验证**：`app/common/auth.py` 中的登录函数
- **会话检查**：`app/__init__.py` 中的 `before_request` 钩子
- **状态API**：`/api/check-account-status` 端点

### 前端
- **状态显示**：员工仪表板顶部的状态指示器
- **JavaScript检查器**：`static/js/account_status_checker.js`
- **自动退出**：检测到注销状态后强制退出

### 数据库
- **状态字段**：`is_active` (Boolean)
- **时间记录**：`deactivated_at` (DateTime)
- **操作记录**：`deactivated_by` (Integer, 外键)

## 📋 测试步骤

### 1. 运行数据库迁移
```bash
python database_migration_add_employee_management.py
```

### 2. 测试安全功能
```bash
python test_account_security.py
```

### 3. 手动测试流程
1. 用高管账号登录
2. 注销一个员工账号
3. 用被注销的员工账号尝试登录
4. 验证无法登录
5. 重新激活账号
6. 验证可以正常登录

## ⚠️ 注意事项

1. **数据安全**：被注销账号的数据不会丢失，只是标记为非活跃
2. **权限控制**：只有高管可以注销下属员工的账号
3. **实时性**：状态变化会立即生效，无需等待
4. **用户体验**：被注销用户会收到清晰的提示信息

## 🐛 故障排除

### 常见问题
1. **无法注销账号**：检查高管权限和员工关系
2. **状态不更新**：刷新页面或检查JavaScript控制台
3. **自动退出不工作**：检查浏览器是否支持JavaScript

### 日志检查
- 查看Flask应用日志
- 检查浏览器开发者工具控制台
- 验证数据库字段是否正确添加

## 📞 技术支持

如果遇到问题，请：
1. 检查错误日志
2. 运行测试脚本
3. 验证数据库迁移状态
4. 联系系统管理员

---

**安全第一，保护您的系统！** 🔐
