# LLRC登录跳转问题修复说明

## 问题描述
首页可以正常显示，但是登录/注册后跳转的页面出现"内部服务器错误"。

## 问题原因
1. **数据库连接问题**：SQLite数据库文件权限或路径问题
2. **路由跳转问题**：登录后重定向到不存在的路由
3. **环境配置问题**：生产环境配置不正确

## 解决方案

### 方法一：使用自动修复脚本（推荐）

1. **上传修复脚本到云服务器**
   ```bash
   # 将 fix_login_redirect_issues.py 上传到云服务器
   scp fix_login_redirect_issues.py root@60.205.251.52:/var/www/llrc/
   ```

2. **在云服务器上运行修复脚本**
   ```bash
   ssh root@60.205.251.52
   cd /var/www/llrc
   su - llrcuser
   python3 fix_login_redirect_issues.py
   ```

### 方法二：手动修复步骤

#### 1. 修复数据库问题
```bash
# 连接到云服务器
ssh root@60.205.251.52
cd /var/www/llrc
su - llrcuser

# 修复数据库权限
sudo chown llrcuser:llrcuser instance/site.db
sudo chmod 644 instance/site.db

# 初始化数据库
source venv/bin/activate
python3 -c "from app import create_app; from app.models import db; app = create_app(); app.app_context().push(); db.create_all()"
```

#### 2. 修复文件权限
```bash
# 修复项目目录权限
sudo chown -R llrcuser:llrcuser /var/www/llrc
sudo chmod -R 755 /var/www/llrc

# 修复特定目录权限
sudo chmod -R 777 /var/www/llrc/flask_session_data
sudo chmod -R 777 /var/www/llrc/instance
```

#### 3. 更新环境配置
```bash
# 更新.env文件
cat > .env << EOF
GOOGLE_API_KEY=AIzaSyDdOylv0bq8q1UypVG-r4m2yHxHNf_CsMo
GEMINI_MODEL=gemini-1.5-flash
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat

# 生产环境配置
FLASK_ENV=production
SECRET_KEY=llrc-secret-key-production-2025
MONGODB_URI=mongodb://localhost:27017/llrc
DATABASE_URL=sqlite:///instance/site.db
EOF

sudo chown llrcuser:llrcuser .env
sudo chmod 644 .env
```

#### 4. 创建测试用户
```bash
# 创建测试用户脚本
cat > create_test_users.py << 'EOF'
from app import create_app
from app.models import User, db
from datetime import datetime

app = create_app()
with app.app_context():
    # 创建测试用户
    users_data = [
        {
            'first_name': '测试', 'last_name': '求职者', 'company_name': '测试公司',
            'email': 'candidate@test.com', 'phone_number': '13800138001',
            'birthday': '1990-01-01', 'password': '123456',
            'user_type': 'candidate', 'is_hr': False
        },
        {
            'first_name': '测试', 'last_name': 'HR', 'company_name': '测试公司',
            'email': 'hr@test.com', 'phone_number': '13800138002',
            'birthday': '1985-01-01', 'password': '123456',
            'user_type': 'recruiter', 'is_hr': True
        },
        {
            'first_name': '测试', 'last_name': '高管', 'company_name': '测试公司',
            'email': 'executive@test.com', 'phone_number': '13800138003',
            'birthday': '1980-01-01', 'password': '123456',
            'user_type': 'executive', 'is_hr': False
        },
        {
            'first_name': '测试', 'last_name': '员工', 'company_name': '测试公司',
            'email': 'employee@test.com', 'phone_number': '13800138004',
            'birthday': '1992-01-01', 'password': '123456',
            'user_type': 'employee', 'is_hr': False,
            'employee_id': 'EMP001', 'supervisor_id': None,
            'hire_date': datetime.now().date()
        }
    ]
    
    for user_data in users_data:
        existing = User.query.filter_by(email=user_data['email']).first()
        if not existing:
            user = User(**user_data)
            db.session.add(user)
    
    db.session.commit()
    print("测试用户创建完成")
EOF

# 执行脚本
source venv/bin/activate
python3 create_test_users.py
rm create_test_users.py
```

#### 5. 重启服务
```bash
# 重启所有服务
sudo systemctl restart llrc
sudo systemctl restart nginx
sudo systemctl restart mongod
```

#### 6. 测试修复结果
```bash
# 测试网站访问
curl -s -o /dev/null -w '%{http_code}' http://localhost/
curl -s -o /dev/null -w '%{http_code}' http://localhost/auth/sign
```

## 测试账号

修复完成后，可以使用以下测试账号：

| 角色 | 邮箱 | 密码 |
|------|------|------|
| 求职者 | candidate@test.com | 123456 |
| HR | hr@test.com | 123456 |
| 高管 | executive@test.com | 123456 |
| 员工 | employee@test.com | 123456 |

## 验证步骤

1. **访问登录页面**：http://60.205.251.52/auth/sign
2. **使用测试账号登录**
3. **检查是否正常跳转到对应页面**
4. **检查各功能模块是否正常**

## 常见问题排查

### 1. 如果仍有错误
```bash
# 查看服务日志
sudo journalctl -u llrc -f

# 查看应用日志
tail -f /var/www/llrc/app.log

# 查看Nginx错误日志
sudo tail -f /var/log/nginx/error.log
```

### 2. 数据库连接问题
```bash
# 检查数据库文件
ls -la /var/www/llrc/instance/

# 重新初始化数据库
cd /var/www/llrc
source venv/bin/activate
python3 -c "from app import create_app; from app.models import db; app = create_app(); app.app_context().push(); db.drop_all(); db.create_all()"
```

### 3. 权限问题
```bash
# 重新设置权限
sudo chown -R llrcuser:llrcuser /var/www/llrc
sudo chmod -R 755 /var/www/llrc
sudo chmod -R 777 /var/www/llrc/instance
sudo chmod -R 777 /var/www/llrc/flask_session_data
```

## 联系支持

如果问题仍然存在，请提供以下信息：
1. 错误日志内容
2. 具体的错误页面截图
3. 使用的测试账号和操作步骤
