# 智能招聘系统虚拟环境使用说明

## 概述
本文档说明如何在本地设置和使用虚拟环境，确保虚拟环境不会被上传到GitHub。

## 重要提醒
⚠️ **虚拟环境目录 (.venv/ 和 venv/) 已经被添加到 .gitignore 文件中，不会被上传到GitHub**

## 快速开始

### 方法1：使用批处理文件（推荐Windows用户）

1. **设置虚拟环境**
   ```
   双击运行 setup_venv.bat
   ```

2. **启动应用**
   ```
   双击运行 start.bat
   ```

### 方法2：使用PowerShell脚本（推荐PowerShell用户）

1. **设置虚拟环境**
   ```powershell
   .\setup_venv.ps1
   ```

2. **启动应用**
   ```powershell
   .\start.ps1
   ```

### 方法3：手动设置

1. **创建虚拟环境**
   ```bash
   python -m venv .venv
   ```

2. **激活虚拟环境**
   - Windows (CMD): `.venv\Scripts\activate.bat`
   - Windows (PowerShell): `.venv\Scripts\Activate.ps1`
   - Linux/Mac: `source .venv/bin/activate`

3. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

4. **启动应用**
   ```bash
   python app\run.py
   ```

## 虚拟环境管理

### 激活虚拟环境
```bash
# Windows CMD
.venv\Scripts\activate.bat

# Windows PowerShell
.venv\Scripts\Activate.ps1

# Linux/Mac
source .venv/bin/activate
```

### 退出虚拟环境
```bash
deactivate
```

### 删除虚拟环境
```bash
# Windows
rmdir /s /q .venv

# Linux/Mac
rm -rf .venv
```

## 常见问题

### Q: 为什么虚拟环境不会被上传到GitHub？
A: 项目根目录的 `.gitignore` 文件已经包含了以下规则：
```
# Virtual environments
.venv/
venv/
env/
```

### Q: 如何确保团队成员都能正确设置虚拟环境？
A: 团队成员只需要运行 `setup_venv.bat` 或 `setup_venv.ps1` 脚本即可自动设置。

### Q: 如果遇到权限问题怎么办？
A: 在PowerShell中运行以下命令：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q: 如何更新依赖？
A: 在激活的虚拟环境中运行：
```bash
pip install -r requirements.txt --upgrade
```

## 项目结构
```
LLRC/
├── .venv/                 # 虚拟环境目录（不提交到Git）
├── app/                   # 应用代码
├── requirements.txt       # Python依赖列表
├── setup_venv.bat        # 虚拟环境设置脚本
├── setup_venv.ps1        # PowerShell设置脚本
├── start.bat             # 启动脚本
├── start.ps1             # PowerShell启动脚本
└── .gitignore            # Git忽略文件
```

## 注意事项

1. **永远不要提交虚拟环境目录到Git**
2. **每个团队成员都需要在本地创建自己的虚拟环境**
3. **确保Python版本兼容性（推荐Python 3.8+）**
4. **定期更新requirements.txt文件**

## 技术支持

如果遇到问题，请检查：
1. Python是否正确安装
2. 虚拟环境是否正确激活
3. 依赖是否正确安装
4. 查看错误日志文件 `app.log`
